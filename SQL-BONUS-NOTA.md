# Bónus Especial — dados

A tabela `bonus_especial_participacoes` já criada continua a ser usada.

Estados:
- `registado` — preencheu nome/contacto, ainda não girou
- `concluido` — já girou a roleta

`cliente_id` fica NULL até o admin clicar em **Tornar Cliente Registado**.

Não é obrigatório alterar o SQL se a tabela já existe.
